// Outra coisa que podemos fazer é ir pra trás. Não necessariamente nossa repetição precisa ir pra frente:

for (let i = 50; i >= 0; i -= 10) { // Enquanto meu i for maior ou igual a 0, ele vai perder - 10 a cada passagem do looping
    console.log(`Linha ${i}`)
}