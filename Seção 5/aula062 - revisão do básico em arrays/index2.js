// Criando o mesmo array utilizando construtor de arrays usando 'new Array' - Não é tão utilizado
// Entre parênteses coloco os elementos do meu array
// Isso funciona com todos os tipos de dados: booleanos, números, strings, etc

const nomes = new Array('Eduardo', 'Maria', 'Joana')
console.log(nomes)